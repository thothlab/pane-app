<?xml version="1.0" encoding="utf-8"?>
<!--
    Pane Helper: a single-Activity APK whose only job is to call
    KeyChain.createInstallIntent() with the CA bytes Pane passes in.
    This bypasses the Android-11+ scoped-storage detour through the
    SAF picker, and the Android-16 Samsung "Shell can't install CAs"
    block, because the install request comes from a real app instead
    of from adb shell.

    No launcher icon, no label entry on the home screen — the user
    should never see this app unless they go looking for it in app
    settings. Launched explicitly by `adb shell am start`.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="Pane Helper"
        android:allowBackup="false"
        android:hasCode="true"
        android:icon="@android:drawable/ic_lock_lock">

        <activity
            android:name=".InstallCaActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>
</manifest>
